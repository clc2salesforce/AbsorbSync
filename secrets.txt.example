# Absorb LMS API Configuration
# Copy this file to secrets.txt and fill in your actual credentials
# Never commit secrets.txt to version control

# Required: Base API URL and API Key
ABSORB_API_URL=https://your-company.myabsorb.com/api/rest/v2
ABSORB_API_KEY=your_api_key

# Optional: REST API token authentication credentials
# If provided, the script will call /authenticate to get a token
# If not provided, will attempt to use API key only
ABSORB_API_USERNAME=your_username
ABSORB_API_PASSWORD=your_password
ABSORB_API_PRIVATE_KEY=your_private_key_guid

# Note: 
# - privateKey is a GUID that identifies the client (request from system owner)
# - The /authenticate endpoint requires all three: username, password, and privateKey
# - See: https://docs.myabsorb.com/integration-api/v2/docs/authentication/
