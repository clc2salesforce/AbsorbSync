# Absorb LMS API Configuration
# Copy this file to secrets.txt and fill in your actual credentials
# Never commit secrets.txt to version control

# Required: All fields are required for authentication

# Base API URL (without /authenticate)
ABSORB_API_URL=https://rest.sandbox.myabsorb.com

# API Key - used for both x-api-key header and privateKey in authentication
ABSORB_API_KEY=your_api_key_guid

# Username and password for authentication
ABSORB_API_USERNAME=your_username
ABSORB_API_PASSWORD=your_password

# Note: 
# - The API key (ABSORB_API_KEY) is used for both the x-api-key header 
#   and as the privateKey in the /authenticate request body
# - All four fields above are required
# - See: https://docs.myabsorb.com/integration-api/v2/docs/authentication/
